#include<vector>
#include<string>
#include<fstream>
#include<iostream>
using namespace std;

using status = string;
using name_ = string;
using file_name = string;
using position_ = char;
using number_t = int;
using token_ = int;

using output_file = std::ofstream ;